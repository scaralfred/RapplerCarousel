import React from 'react';
import { render } from 'react-dom';
import RapplerCarousel from '../../src';
const App = () => (
    <RapplerCarousel />
);
render(<App />, document.getElementById("rappler_carousel"));